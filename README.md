# nu-issue-recursion-loop-ctrl-c

Run:

```bash
nu loop.nu
```

**Expected**: be able to halt script exection with <kbd>Ctrl+C</kbd>

**Actual**: you cannot do so, script continues its execution anyway
